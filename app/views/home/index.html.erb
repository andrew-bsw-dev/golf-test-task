

<h2>Hoverable Table</h2>
<p>Move the mouse over the table rows to see the effect.</p>

<table>
  <tr>
    <% @slots_start.each_with_index do |slot_start, index| %>
      <th><%= "#{slot_start} to #{@slots_end[index]}" %></th>
    <% end %>
  </tr>
  <tr>
    <td>Peter</td>
    <td>Griffin</td>
    <td>$100</td>
  </tr>
</table>

<%= link_to 'Graph-QL', graphql_path, method: :post %>

<style>
  table {
    border-collapse: collapse;
    width: 100%;
  }

  th, td {
    padding: 8px;
    text-align: left;
    border-bottom: 1px solid #DDD;
  }

  tr:hover {background-color: #D6EEEE;}
</style>